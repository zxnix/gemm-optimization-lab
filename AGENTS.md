# Codex 项目协作规范

## 定位与语言

本项目以 GEMM 为实验对象，研究 C++、编译器 IR、机器指令与 CPU/GPU 执行，不以制作通用矩阵库为目标。面向用户的解释、实验记录和学习笔记使用中文；标识符、命令、标准术语和提交信息使用英文。新概念首次出现时给出中英文名称。

## 解释与注释

- 每项实质工作说明背景、问题、假设、方案、实现、验证、benchmark、结果、局限和下一步。
- 公共 API 用 Doxygen 注释说明数学语义、形状、布局、精度、写入语义、前置条件和异常。
- 注释应解释 row-major、stride、spatial/reduction axis、数值误差、计时边界、cache、SIMD、线程机制和有意保留的低效行为。
- 注释“为什么”，不要把显然的 C++ 语句翻译成自然语言；长篇原理放 `docs/`，局部约束放源码附近。

## 实验纪律

- 永久保留 naive kernel；一次只改变一个主要变量。
- 新 kernel 复用同一正确性验证和 benchmark 协议。
- 报告单次耗时、mean、median、GFLOP/s、误差、CPU、Cache、系统、编译器和参数。
- 分配、初始化、输出和验证不进入 kernel 计时区间。
- 没有测量证据时不宣称提升；WSL 结果只作开发基线，论文级结论在受控原生 Linux 复现。
- 不提交构建目录、二进制文件或无关生成物。
